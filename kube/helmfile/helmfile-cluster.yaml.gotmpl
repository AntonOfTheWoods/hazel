environments:
  default:
  dev:
  production:
---

repositories:
- name: ot-helm
  url: https://ot-container-kit.github.io/helm-charts/

helmDefaults:
  wait: true
  waitForJobs: true

templates:
{{ readFile "blocks/argo-tmpl.yaml" | indent 2 }}
{{ readFile "blocks/cert-manager-tmpl.yaml" | indent 2 }}
{{ readFile "blocks/cnpg-tmpl.yaml" | indent 2 }}
{{ readFile "blocks/traefik-tmpl.yaml" | indent 2 }}
{{ readFile "blocks/garage-tmpl.yaml" | indent 2 }}
{{ readFile "blocks/redis-tmpl.yaml" | indent 2 }}

releases:
  - <<: *garage
  - <<: *redis
  - <<: *argo
    values:
      - "./values/argo-values.yaml"
  - <<: *cert-manager
  - <<: *trust-manager
  - <<: *cnpg
  - <<: *plugin-barman-cloud
  - <<: *traefik
    values:
      - "./values/traefik-values.yaml"
