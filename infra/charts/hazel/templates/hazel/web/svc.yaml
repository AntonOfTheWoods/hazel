{{- if and .Values.hazel.enabled .Values.hazel.web.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.hazel.web.service.name | default "web" }}
  annotations:
    {{- include "hazel.annotations" . | nindent 4 }}
  labels:
    {{- include "hazel.componentLabels" (dict "component" "web" "root" .) | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.hazel.web.service.port | default 8000 }}
    protocol: TCP
    targetPort: {{ .Values.hazel.web.application.port | default 8000 }}
  selector:
    {{- include "hazel.componentSelectorLabels" (dict "component" "web" "root" .) | nindent 4 }}
status:
  loadBalancer: {}
{{- end -}}
