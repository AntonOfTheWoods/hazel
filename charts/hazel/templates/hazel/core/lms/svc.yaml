{{- if and .Values.hazel.enabled .Values.hazel.lms.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.hazel.lms.service.name | default "lms" }}
  annotations:
    {{- include "hazel.annotations" . | nindent 4 }}
  labels:
    {{- include "hazel.componentLabels" (dict "component" "lms" "root" .) | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.hazel.lms.service.port | default 8000 }}
    protocol: TCP
    targetPort: {{ .Values.hazel.lms.application.port | default 8000 }}
  selector:
    {{- include "hazel.componentSelectorLabels" (dict "component" "lms" "root" .) | nindent 4 }}
status:
  loadBalancer: {}
{{- end -}}
